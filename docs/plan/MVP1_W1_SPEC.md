# AgentGaia — MVP1 Week 1 Spec

**목표:** 경영진 첫 시연에서 "오, 이거 되네" 킥  
**기간:** 5일 (월~금)  
**핵심:** 채팅 + 파일 업로드 + RAG Q&A

---

## 1. W1 킥 포인트

```
"ChatGPT는 우리 파일 못 읽잖아"
        ↓
"여기는 파일 넣으면 그 안에서 찾아서 답해줘"
        ↓
"오, 이거 되네"
```

**시연 시간:** 3분 이내  
**시연 흐름:** 파일 드롭 → 요약 → 질문 → 출처까지

---

## 2. 시연 대본 (3분)

### Scene 1: 첫인상 (30초)

```
[시연자] 
"채팅창입니다. 여기에 파일 넣으면 분석해줍니다."
(30페이지 PPT 드래그앤드롭)

[화면]
📎 Q3_발표자료.pptx 업로드 중...
✓ 완료. 무엇을 도와드릴까요?
```

### Scene 2: 요약 (1분)

```
[시연자] 
"요약해줘" (타이핑)

[화면]
🤔 분석 중...

📋 핵심 요약:
1. 3분기 매출 목표 150억 (전분기 대비 +11%)
2. 전략: B2B 신규 20개사 확보
3. 리스크: 원자재 가격 상승

[시연자]
"30페이지가 3줄로 됐습니다."
```

### Scene 3: Q&A - 킥 (1분)

```
[시연자]
"신규 고객 확보 방안이 뭐야?" (타이핑)

[화면]
🔍 문서에서 검색 중...

📋 답변:
신규 고객 확보 방안:
- 산업별 타겟 마케팅 (제조, 물류 중심)
- 기존 고객 레퍼런스 활용
- 파트너사 공동 영업

📍 출처: 슬라이드 15-16

[경영진 예상 반응]
"오, 파일 안에서 찾아서 답해주네"
"출처도 나오네"
```

### Scene 4: 마무리 (30초)

```
[시연자]
"이렇게 파일 넣고 대화하면 됩니다."
"다음 주에는 Word, Excel도 되게 할게요."

[경영진 예상 반응]
"다른 파일도 되면 좋겠네"
→ W2 떡밥 완료
```

---

## 3. 기술 스펙

### 3.1 시스템 구성

```
┌────────────────────────────────────────────┐
│           Streamlit Chat UI                │
│  ┌──────────────────────────────────────┐  │
│  │  [📎 파일 드롭]                       │  │
│  │                                      │  │
│  │  👤 요약해줘                          │  │
│  │  🤖 핵심 요약: 1. 매출... 2. 전략...  │  │
│  │                                      │  │
│  │  👤 신규 고객 확보 방안이 뭐야?        │  │
│  │  🤖 답변: ... (출처: 슬라이드 15)     │  │
│  │                                      │  │
│  │  [메시지 입력...]            [전송]   │  │
│  └──────────────────────────────────────┘  │
└────────────────────────────────────────────┘
                      │
                      ▼
┌────────────────────────────────────────────┐
│              FastAPI Backend               │
│                                            │
│  POST /upload                              │
│    → 파일 저장                              │
│    → PPT 파싱 (python-pptx)                │
│    → 슬라이드별 텍스트 추출                  │
│    → Chunking                              │
│    → Embedding Server 호출                  │
│    → Qdrant 저장                           │
│                                            │
│  POST /chat                                │
│    → Query Embedding                       │
│    → Qdrant 검색 (Top 5)                   │
│    → Context 구성                          │
│    → LLM 호출 (Claude)                     │
│    → 출처 포함 응답                         │
└────────────────────────────────────────────┘
          │                    │
          ▼                    ▼
┌──────────────────┐  ┌──────────────────┐
│ Embedding Server │  │     Qdrant       │
│   (기존 운영)     │  │   (기존 운영)    │
│   POST /embed    │  │                  │
└──────────────────┘  └──────────────────┘
```

### 3.2 API 설계

#### POST /upload

```yaml
Request:
  - file: UploadFile (pptx)

Process:
  1. 파일 저장 (/tmp/{session_id}/{filename})
  2. PPT 파싱
     - 슬라이드별 제목 + 본문 추출
     - 메타데이터: slide_number, title
  3. Chunking
     - 슬라이드 단위 (자연스러운 구분)
     - 너무 길면 500자 단위 분할
  4. Embedding
     - 기존 서버 호출: POST /embed
     - Input: texts[]
     - Output: vectors[]
  5. Qdrant 저장
     - Collection: {session_id}
     - Payload: text, slide_number, title

Response:
  {
    "status": "success",
    "filename": "Q3_발표자료.pptx",
    "slides": 30,
    "chunks": 45,
    "message": "완료. 무엇을 도와드릴까요?"
  }
```

#### POST /chat

```yaml
Request:
  {
    "session_id": "abc123",
    "message": "신규 고객 확보 방안이 뭐야?"
  }

Process:
  1. Query Embedding
     - 기존 서버 호출
  2. Qdrant 검색
     - Collection: {session_id}
     - Top 5 chunks
  3. Context 구성
     - Retrieved chunks → prompt에 삽입
  4. LLM 호출
     - System: "문서 내용 기반으로 답변. 출처 표시."
     - User: context + question
  5. 응답 파싱
     - 출처 (슬라이드 번호) 추출

Response:
  {
    "answer": "신규 고객 확보 방안은...",
    "sources": [
      {"slide": 15, "title": "영업 전략"},
      {"slide": 16, "title": "타겟 고객"}
    ]
  }
```

### 3.3 데이터 흐름

```
[파일 업로드]
    │
    ▼
┌─────────────────┐
│   PPT Parser    │ → 슬라이드별 텍스트
│  (python-pptx)  │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│    Chunker      │ → 검색 단위로 분할
│ (슬라이드 기준)  │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Embedding Server│ → 벡터 변환
│   (기존 운영)    │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│     Qdrant      │ → 저장
│   (기존 운영)    │
└─────────────────┘


[질문 입력]
    │
    ▼
┌─────────────────┐
│ Embedding Server│ → 질문 벡터화
└─────────────────┘
    │
    ▼
┌─────────────────┐
│     Qdrant      │ → 유사 청크 검색
└─────────────────┘
    │
    ▼
┌─────────────────┐
│   LLM (Claude)  │ → 답변 생성
└─────────────────┘
    │
    ▼
[답변 + 출처]
```

### 3.4 프롬프트 설계

#### 요약 요청 시

```
System:
당신은 문서 분석 전문가입니다.
주어진 문서 내용을 바탕으로 핵심을 요약합니다.
반드시 문서에 있는 내용만 사용합니다.

User:
[문서 내용]
{전체 텍스트 또는 주요 청크}

[요청]
이 문서를 3줄로 요약해주세요.
```

#### Q&A 시

```
System:
당신은 문서 기반 Q&A 전문가입니다.
주어진 문서 내용만을 바탕으로 질문에 답변합니다.
답변 마지막에 출처(슬라이드 번호)를 표시합니다.
문서에 없는 내용은 "문서에 해당 내용이 없습니다"라고 답합니다.

User:
[관련 문서 내용]
---
슬라이드 15 (영업 전략):
신규 고객 확보를 위해 산업별 타겟 마케팅 진행...

슬라이드 16 (타겟 고객):
제조, 물류 산업을 1차 타겟으로 선정...
---

[질문]
신규 고객 확보 방안이 뭐야?
```

---

## 4. 일별 개발 계획

### Day 1 (월): 뼈대

| 시간 | 태스크 | 산출물 |
|------|--------|--------|
| 오전 | FastAPI 프로젝트 셋업 | /upload, /chat 엔드포인트 스켈레톤 |
| 오후 | Streamlit 채팅 UI | 파일 업로드 + 채팅창 기본 |
| 저녁 | 연동 테스트 | UI → Backend 통신 확인 |

**Day 1 완료 기준:**
- [ ] 파일 업로드하면 Backend에서 받음
- [ ] 채팅 입력하면 Backend 호출됨
- [ ] (응답은 하드코딩 OK)

### Day 2 (화): 파싱 + 임베딩

| 시간 | 태스크 | 산출물 |
|------|--------|--------|
| 오전 | PPT 파서 구현 | 슬라이드별 텍스트 추출 |
| 오후 | 기존 Embedding 서버 연동 | 텍스트 → 벡터 |
| 저녁 | Qdrant 저장 | 벡터 + 메타데이터 저장 |

**Day 2 완료 기준:**
- [ ] PPT 업로드 → 슬라이드별 텍스트 추출됨
- [ ] 텍스트 → 임베딩 벡터 변환됨
- [ ] Qdrant에 저장됨 (Qdrant UI에서 확인)

### Day 3 (수): RAG

| 시간 | 태스크 | 산출물 |
|------|--------|--------|
| 오전 | Qdrant 검색 구현 | 질문 → Top 5 청크 |
| 오후 | LLM 연동 (Claude) | Context + 질문 → 답변 |
| 저녁 | 출처 표시 | 슬라이드 번호 파싱 |

**Day 3 완료 기준:**
- [ ] 질문하면 관련 청크 검색됨
- [ ] LLM이 답변 생성함
- [ ] 출처(슬라이드 번호) 표시됨

### Day 4 (목): 통합 + 디버깅

| 시간 | 태스크 | 산출물 |
|------|--------|--------|
| 오전 | 전체 플로우 통합 | 업로드 → 질문 → 답변 E2E |
| 오후 | 버그 수정 | 엣지 케이스 처리 |
| 저녁 | 시연 리허설 1차 | 시연 대본대로 테스트 |

**Day 4 완료 기준:**
- [ ] 시연 시나리오 1회 성공
- [ ] 에러 없이 3분 시연 가능

### Day 5 (금): 폴리싱 + 시연

| 시간 | 태스크 | 산출물 |
|------|--------|--------|
| 오전 | UI 다듬기 | 로딩 표시, 에러 메시지 |
| 오후 | 시연 리허설 3회 | 대본 숙지, 백업 시나리오 |
| 저녁 | **경영진 시연** | 🎯 |

**Day 5 완료 기준:**
- [ ] 시연 3회 연속 성공
- [ ] 백업 시나리오 준비됨
- [ ] 경영진 "오, 이거 되네" 반응

---

## 5. 기술 상세

### 5.1 PPT 파싱 코드 (예시)

```python
from pptx import Presentation

def parse_pptx(file_path: str) -> list[dict]:
    prs = Presentation(file_path)
    slides = []
    
    for idx, slide in enumerate(prs.slides, 1):
        text_parts = []
        title = ""
        
        for shape in slide.shapes:
            if shape.has_text_frame:
                text = shape.text_frame.text.strip()
                if shape == slide.shapes.title:
                    title = text
                elif text:
                    text_parts.append(text)
        
        slides.append({
            "slide_number": idx,
            "title": title,
            "content": "\n".join(text_parts)
        })
    
    return slides
```

### 5.2 Qdrant 연동 (예시)

```python
from qdrant_client import QdrantClient
from qdrant_client.models import VectorParams, Distance, PointStruct

client = QdrantClient(host="qdrant-server", port=6333)

# 컬렉션 생성
def create_collection(session_id: str, vector_size: int = 1536):
    client.create_collection(
        collection_name=session_id,
        vectors_config=VectorParams(
            size=vector_size,
            distance=Distance.COSINE
        )
    )

# 저장
def store_chunks(session_id: str, chunks: list[dict], vectors: list[list[float]]):
    points = [
        PointStruct(
            id=i,
            vector=vectors[i],
            payload={
                "text": chunks[i]["content"],
                "slide_number": chunks[i]["slide_number"],
                "title": chunks[i]["title"]
            }
        )
        for i in range(len(chunks))
    ]
    client.upsert(collection_name=session_id, points=points)

# 검색
def search(session_id: str, query_vector: list[float], top_k: int = 5):
    results = client.search(
        collection_name=session_id,
        query_vector=query_vector,
        limit=top_k
    )
    return results
```

### 5.3 디렉토리 구조

```
agentgaia-mvp1/
├── backend/
│   ├── main.py              # FastAPI 앱
│   ├── routers/
│   │   ├── upload.py        # /upload 엔드포인트
│   │   └── chat.py          # /chat 엔드포인트
│   ├── services/
│   │   ├── parser.py        # PPT 파싱
│   │   ├── embedder.py      # Embedding 서버 연동
│   │   ├── vector_store.py  # Qdrant 연동
│   │   └── llm.py           # Claude API
│   └── requirements.txt
│
├── frontend/
│   └── app.py               # Streamlit 채팅 UI
│
└── README.md
```

---

## 6. 리스크 & 백업

| 상황 | 대응 |
|------|------|
| PPT 파싱 실패 | "일부 슬라이드 형식이 지원되지 않습니다" + 파싱된 것만 진행 |
| Embedding 서버 다운 | 사전 테스트로 방지, 시연 전 헬스체크 |
| LLM 환각 | 출처 강조, "확인 필요" 문구 |
| 네트워크 느림 | 시연용 PPT 미리 업로드해두기 |
| 시연 중 에러 | "개발 중 버전이라 가끔 이래요" + 재시도 |

---

## 7. 시연 전 체크리스트

### 하루 전
- [ ] 시연용 PPT 준비 (30페이지, 실제 업무 문서)
- [ ] 예상 질문 3개 미리 테스트
- [ ] Embedding 서버 상태 확인
- [ ] Qdrant 상태 확인
- [ ] LLM API 키 유효성 확인

### 시연 30분 전
- [ ] 서버 재시작 (깨끗한 상태)
- [ ] 브라우저 캐시 클리어
- [ ] 네트워크 연결 확인
- [ ] 화면 공유 테스트
- [ ] 백업 시나리오 준비

### 시연 직전
- [ ] 심호흡
- [ ] 자신감

---

## 8. W2 이후 (이것도 되네)

| Week | 추가 기능 | 킥 |
|:---:|----------|-----|
| W2 | Word, Excel, PDF | "다른 파일도 되네" |
| W3 | 다중 파일 비교 | "여러 개도 되네" |
| W4 | UI 완성도 | "영업 가능하겠다" |

W2부터는 기반이 있으니까 확장만 하면 됨.
W1이 성공하면 나머지는 쉬움.

---

**문서 버전:** 1.0  
**작성일:** 2025-12-03  
**D-Day:** W1 금요일 시연
